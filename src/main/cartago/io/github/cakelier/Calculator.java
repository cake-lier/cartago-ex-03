package io.github.cakelier;

import cartago.Artifact;
import cartago.OPERATION;

public class Calculator extends Artifact {

    @OPERATION
    public void add(final double firstAddend, final double secondAddend, /* Add the output parameter here. */) {
        /* Add the setting of the output parameter here. */
    }

    @OPERATION
    public void sub(final double minuend, final double subtrahend, /* Add the output parameter here. */) {
        /* Add the setting of the output parameter here. */
    }
}
